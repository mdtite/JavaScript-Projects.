function myFunction () {
    var sentance = "I am learning" //creating var
    sentance += " how to code!" //using the += operator to add to the var
    document.getElementById("concatenate").innerHTML = sentance //usung get element by id to change the text
}